#include "stdafx.h"
#include "stanalyzer.h"

int stanalyzer(stTree &ret)
{
	idListLayer *correct = new idListLayer;
	idList idTable;
	idTable.push_back(correct);
	stTree::iterator p, pEnd = ret.end();
	for (p = ret.begin(); p != pEnd; p++)
	{

	}
	return -1;
}